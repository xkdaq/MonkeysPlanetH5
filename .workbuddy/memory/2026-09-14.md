# 2026-09-14

## 教育学帽子题消消乐 · 改版对齐站点设计语言
- 文件：`public/html/333/教育学帽子题消消乐.html`（只改 public/ 源，不动 dist/）
- 配色全量换成站点主站 token：品牌绿 #11b981 / #07C160（--brand-dark #067647、--brand-soft #ecfdf3、--brand-border #b7e4cd），文字梯度 #101828 / #344054 / #667085 / #98a2b3，描边 #e7eaee，卡片纯白 #fff。
- 题干卡沿用站点表格「典型题目」的蓝色系（#f0f6ff / #1d4ed8 / 标签 #2563eb），答案卡用品牌绿系（#ecfdf3 / #067647 / 标签 #11b981）；得分用琥珀 #d9a441，错误用 #d92d20。
- 圆角统一 12/16px、胶囊 999px；阴影统一 0 14px 30px rgba(16,24,40,.07)；卡片 hover translateY(-2px)、:active scale(.985)，过渡 .16s ease（对齐 style.css 的 .pan-card 手感）。
- 交互新增（不碰逻辑）：顶部导航条（回到首页 study.monkeysxu.top + 品牌署名）、底部 footer（含鄂ICP备2024066684号-4，与大转盘一致）、数字变化时 statPop 放大动效、连击 >=3 转琥珀色、胶囊按钮/胶囊 chip 开关、进度条渐变发光。
- 题库与逻辑零改动：12 关 96 组、存档 KEY 不变、夜间模式/音效/对答案开关/提示/错题本/跳关全保留。仅新增 `flash()` 与 `_pv` 用于视觉反馈。
- 冒烟：headless Chrome 验证正确配对（score10/combo1/remain7/acc100%/双卡 gone）、错误配对（remain 不变、错题+1、提示高亮 2 张、选中态复位）均正常，无 JS 报错。
- 关卡条上的冗余修复：手机上 lv-name 里的关名胶囊 `<em id="vLevelName">` 与触发按钮 `#lvPick` 显示同一关名，互相挤压只剩"中国..."。≤640px 直接 `display:none` 掉 em，仅保留"第 X 关 / 共 N 关"，关名由触发按钮承载。桌面（≥701px）em 照常显示保持紧凑信息层。
- 删底部页脚：去掉「猴子考研 · 学习站 · 鄂ICP备2024066684号-4」整行（与上一轮我加的备案信息），底部只剩三个功能按钮 + 快捷键提示。游戏功能复测通过。
- 修复 buildJump 内部 `const unlocked=i<=unlocked` 变量遮蔽导致 TDZ 报错（外层 let unlocked），改名为 `isOpen`。
- 桌面/手机综合冒烟：配对（score10/remain7/acc100%/双卡 gone）+ 弹层（opts=12/active=1/trig 显示当前关/veilon=true/pickOpen=true）三档宽度正常。
- 改名：游戏名「消消乐」→「连连看」（更贴合配对玩法）。文件已由 `public/html/333/教育学帽子题消消乐.html` 重命名为 `教育学帽子题连连看.html`（用户确认接受旧 URL 失效；该文件未被站内任何页面引用，git 也未跟踪）。注意：dist/ 需重新 build。
- 注意：dist/ 停留在 2026-08-31，尚未包含本页，部署前需执行 `npm run build`。