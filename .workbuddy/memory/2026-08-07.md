# 2026-08-07

- MonkeysPlanetH5 静态页面托管方案：用户把独立 HTML 放在 `public/html/` 下（如 cet/、333/ 子目录），Vite 构建时自动原样拷贝到 `dist/html/`，部署后通过 `域名/html/xxx.html` 访问。
- 修复 vite.config.js：原来 rollupOptions.input 里登记了根目录的 cet-practice-2026.html / cet-writing-2026.html，文件被移入 public/html/cet/ 后导致构建报 UNRESOLVED_ENTRY。已删去这两个入口，仅保留 index.html 主入口。
- 约定：独立静态 HTML 一律放 public/ 下，不要登记为 rollup input（否则会被打包处理且路径变动会挂构建）。
- 外链笔记（type===1 且有 linkUrl）交互改版（已获用户确认）：点击不再 `window.location.href` 直接跳走，改为站内笔记详情页（仍走 `?type=note&id=` + openDetail，URL 可刷新还原），内容区为**全屏内嵌 iframe**（`.link-detail-full`，100dvh-46px，`width:100vw; margin-left:calc(50% - 50vw)` 突破 page-body max-width），仅保留顶部返回栏 + 右下角「浏览器打开」浮动兜底按钮。openDetail 增加第 4 个参数 seedData（列表数据兜底，接口失败或字段缺失时仍可用）。外链笔记隐藏画板按钮（isExternalLinkNote 计算属性）。注：第一版卡片式内嵌 UX 差（双重滚动条、桌面端窄条），被否后改为全屏方案。
