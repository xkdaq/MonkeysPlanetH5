# 2026-08-19

- 14:25 新增赞赏名单（感谢墙）功能，方案：静态 JSON（后续记录增多可改后端接口）。
  - 数据：`public/data/donations.json`（name/amount/date/message 四字段，新的加在数组最前）
  - 接口层：`src/api/donations.js` fetchDonations()，封装了 JSON 读取，将来换接口只改这一个文件
  - 组件：PersonalCenter.vue 切到 donate 视图时加载（watch view），列表为空不渲染
  - 样式：style.css `.pc-donate-thanks*` 金色系（边框 #f0e2c0、背景 #fffdf7、金额 #c9932b），与赞赏码海报呼应
  - vite build 通过
- 14:40 赞赏名单昵称脱敏：组件 maskName() 保留首字符、其余补 *，空名显示「匿名」；展示层处理，源JSON可继续存全名。vite build 通过。
