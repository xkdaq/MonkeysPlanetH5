# 2026-08-22 工作日志

## 大转盘 v20260822.1 — 4 项需求一次性升级

### 改动
- 单文件 `public/html/333/大转盘.html`
- 后端：**零改动**。事件复用通用统计 `/h5/visit/report`（Spring @Anonymous，CORS 已放开，curl 实测 200）
- 前端：内联轻量 `_track()` 上报函数，不再依赖 Vite 模块

### 4 项需求实现
1. **题库下拉美化（低）** — 原生 `<select>` 改造为底部弹层（含遮罩 + 列表 + 序号 + 当前选中对勾），点击触发器弹出，点选项或外部关闭
2. **短间隔思考提示（低）** — `spin()` 完成记 timestamp，`toggleBtn` 检测距上次 spin < 3s 且未提示过则弹「先思考一下」吐司，1.6s 自动消失
3. **公众号 / 首页（中）**
   - `<title>` 默认值改为「公众号：猴子考研 · 抽题大转盘」
   - 头部加 brand chip「公众号：猴子考研 · +绿点」
   - 头部右上角加「首页」按钮链 `https://study.monkeysxu.top`，SVG home icon
   - 页脚加品牌 chip + 首页链接 + ICP 备案号「鄂ICP备2024066684号-4」
4. **H5 统计上报（高）** — 5 类事件全部复用 `reportVisit()`，`eventType` 自定义（数据库 varchar 不限）
   - `page_view` 进入即发
   - `wheel_bank_load` 题库列表加载完
   - `wheel_bank_switch` 切换题库
   - `wheel_spin` 转盘完成转动（含 materialId 题库id + materialTitle 题库名）
   - `wheel_view_analysis` 打开解析

### 关键验证
- grep 明文 key「47ccmu」「K5i9Tb」= 0 出现（脱敏依旧）
- grep `_track(` = 6 处（定义1 + 调用5）
- vite build 成功；curl /h5/visit/report 200 `{code:0}`

### 注意事项
- H5VisitLog.eventType 是 varchar，没枚举约束，可自定义。前提：写日志要足够精确（pagePath + materialTitle 二维标识来源），后台 SQL 写查询时能筛对。
- 重新打包 dist 已就位（js 编译 224ms），无需后端部署；前端部署 `dist/html/333/大转盘.html` 即可。

## ⚠️ 加密 JAR 部署待确认
- 上一轮为转盘接口加密生成的新 JAR 含「手动写响应返回 null 跳过 Spring converter」修复，部署后请用 curl 验证返回 `Content-Type: text/plain` + body 无外层双引号
- 新版前端版本号 v20260822.1 含解密逻辑（key 仍混淆）

## 接口与版本
- API_BASE = `https://api.monkeysxu.top`
- STUDY_HOME = `https://study.monkeysxu.top`
- 大转盘 dist 路径：`dist/html/333/大转盘.html`
- 上一版 wheel-v20260821.2 已弃用

## 大转盘 v20260822.2 — 去掉顶部公众号露出

### 改动
- 去掉头部 brand chip「公众号：猴子考研」
- `<title>` 和默认标题改为「抽题大转盘」（不再带「公众号：猴子考研 · 」前缀）
- 选择器触发按钮固定显示「选择题库」，当前题库名放到 `<h1>` 标题中
- 触发按钮去掉 emoji 图标

### 验证
- grep 确认「公众号：猴子考研 · 」「📚」「bank-icon」在 dist 中均未出现
- vite build 成功（203ms）

### 待部署
- 前端部署 `dist/html/333/大转盘.html` 到 study 域名，强刷后版本号 v20260822.2

## 大转盘 v20260822.3 — UI 微调（首页按钮左移 / 选择器高度 / 页脚精简）

### 改动
- 头部「首页」按钮从右上角移到左上角
- 题库选择器触发按钮高度压低：`padding` 从 `11px 16px` 改为 `8px 14px`，圆角从 `14px` 改为 `12px`
- 页脚只保留版本号 + ICP 备案号：去掉公众号 brand、去掉 study 链接、去掉题库来源描述

### 验证
- grep 确认 `footer-brand`、`公众号：猴子考研`、`study.monkeysxu.top` 在页脚已不出现
- vite build 成功

### 待部署
- 前端部署 `dist/html/333/大转盘.html` 到 study 域名，强刷后版本号 v20260822.3

## 大转盘 v20260822.4 — 首页按钮改为视口固定

### 改动
- 头部「首页」按钮从 `position: absolute`（相对 .header）改为 `position: fixed`（相对视口），`top: calc(env(safe-area-inset-top, 0px) + 12px)`，避免与标题挤一行
- 背景加半透明深色 + 毛玻璃，固定后滚动不消失
- .header 去掉 `position: relative`，padding-top 加 safe-area-inset-top 适配刘海屏

### 验证
- vite build 成功
- 版本号 v20260822.4

### 待部署
- 前端部署 `dist/html/333/大转盘.html` 到 study 域名，强刷后版本号 v20260822.4

## 大转盘 v20260822.5 — 标题左移避让 fixed 首页按钮

### 改动
- .header h1 和 .header p 加 `padding-left: 88px`，给左上角 fixed 按钮留出空间
- h1 和 p 改为 `text-align: left`（配合左移）
- .header 恢复 `position: relative`（保留，不影响 fixed）

### 验证
- vite build 成功
- 版本号 v20260822.5

### 待部署
- 前端部署 `dist/html/333/大转盘.html` 到 study 域名，强刷后版本号 v20260822.5

## 大转盘 v20260822.6 — 标题恢复居中，顶部加 padding 避让

### 改动
- 回退 v20260822.5 的左对齐：h1 / p 恢复 `text-align: center`，去掉 `padding-left: 88px`
- .header padding-top 从 `24px` 增加到 `48px`，给左上角 fixed 按钮留出自然空白，不重叠
- .header 保持 `position: relative`（v5 误加，无影响）

### 验证
- vite build 成功
- 版本号 v20260822.6

### 待部署
- 前端部署 `dist/html/333/大转盘.html` 到 study 域名，强刷后版本号 v20260822.6

## 大转盘 v20260822.7 — 错误提示改为垂直堆叠

### 改动
- .empty-hint 改为 `display: flex; flex-direction: column; align-items: center; gap: 12px;`
- .retry-btn 去掉 `margin-top: 12px`（gap 已控制间距）
- 效果：报错文字在上，「重新加载」按钮在下，垂直排列

### 验证
- vite build 成功
- 版本号 v20260822.7

### 待部署
- 前端部署 `dist/html/333/大转盘.html` 到 study 域名，强刷后版本号 v20260822.7

## 大转盘 v20260822.8 — 页脚固定到底部

### 改动
- .footer 加 `margin-top: auto` + `width: 100%`，利用 body 的 `min-height: 100vh` + flex 布局自动沉底
- 报错时页脚不再飘在页面中间

### 验证
- vite build 成功
- 版本号 v20260822.8

### 待部署
- 前端部署 `dist/html/333/大转盘.html` 到 study 域名，强刷后版本号 v20260822.8

## 大转盘 v20260822.9 — 不重复抽题（按题库隔离）+ 浏览器标识

### 背景
- 用户问「浏览器有没有唯一标识」+ 想要「开关键下同一浏览器抽过的题不再重复」
- 结论：浏览器无硬件级设备 ID，用 localStorage 生成随机 UUID（`wheel_browser_id`）作为本浏览器标识；清缓存/无痕会丢，日常够用

### 改动
- 选择题库下方新增开关「抽过的题不再重复」（iOS 风格微信绿 slider）+ 「重置本题库」按钮
- 开关状态存 `wheel_no_repeat`（'1'/'0'），刷新保持
- 已抽题按题库隔离存 `wheel_drawn_{bankId}`（题 id 数组）
- spin 逻辑：开开关时从「未抽池」随机选，转到的仍是全量扇区（视觉一致）；某题库全部抽完弹 toast 提示重置
- showQuestion 抽中后（开开关）写 `addDrawn`
- `getBrowserId()` 接入 `page_view` 与 `wheel_spin` 上报，后台可按 browserId 去重统计设备
- 版本号升 v20260822.9（meta + JS 两处已统一）

### 验证
- dist 版本 v20260822.9；关键函数存在；明文 key 0 出现

### 待部署
- 前端部署 `dist/html/333/大转盘.html` 到 study 域名，强刷后版本号 v20260822.9

## 大转盘 v20260822.10 — 显示已抽数量

### 背景
- 用户问「在哪里可以看到已抽记录」；实际记录存在 localStorage，但 UI 没展示

### 改动
- 开关右侧新增 `drawnCount`：开关打开且已加载题库时显示「（已抽 x/y）」
- 更新点：切换开关、选择题库、抽中题、重置本题库时都会刷新数量
- 截图中 Console 报错 `A listener indicated an asynchronous response...` 来自浏览器扩展，非页面代码问题
- 版本号升 v20260822.10

### 验证
- dist 版本 v20260822.10；drawnCount/updateDrawnCount 存在

### 待部署
- 前端部署 `dist/html/333/大转盘.html` 到 study 域名，强刷后版本号 v20260822.10