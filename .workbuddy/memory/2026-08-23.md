# 2026-08-23 工作日志

## 大转盘（MonkeysPlanetH5/public/html/333/大转盘.html）改版 v20260823.1
- 用户反馈顶部「选择题库 + 不重复开关」太挤，要求右上角加「更多」按钮，把这些功能收进面板，并在面板内新增「抽题历史」。
- 实现：右上角固定「更多」按钮（.header-more），底部弹出 .more-sheet 面板，内含三段：①选择题库（复用 bank-trigger，点开先隐藏更多再弹 bank-sheet）；②不重复抽题开关+重置+已抽x/y；③抽题历史（localStorage `wheel_history`，记录题库名/题目标题/时间，最多200条，列表可点击重开该题、可清空）。
- 顶部仅保留标题区，选择题库不再占首屏空间。
- 抽题历史点击重开：跨题库时先 await selectBank 再 showQuestion(idx, true)，fromHistory=true 避免重复记历史。
- 已 vite build 到 dist；校验版本号、历史函数、更多面板元素、明文密钥零出现均通过。待部署：study 域名强刷 dist/html/333/大转盘.html。

## 大转盘改版 v20260823.2（用户纠偏）
- 用户要求：选择题库放回顶部外面，抽题历史改成二级弹框（不在更多面板内联）。
- 调整：①顶部恢复 `.bank-select`（选择题库触发按钮）；②更多面板只保留「不重复抽题开关 + 抽题历史入口行」；③新增独立 `.history-sheet`（z-index 1100，高于 more 的 1000）作为二级弹框，含历史列表+清空+关闭；点历史条目先关两个弹框再重开该题。
- bindBankSheet 还原为顶部触发按钮的 show/hide 切换；showMoreSheet 仅刷新入口计数。
- 已 vite build；dist 校验通过（bank-select 仅顶部 1 处、historySheet/Row/Mask 齐全、明文密钥 0）。待部署强刷。

## 大转盘微调 v20260823.3
- 用户截图反馈「更多」面板里「抽题历史」行数量与箭头分散。
- 调整：把数量包进右侧分组（.more-row-right），和箭头贴在一起靠右显示；左侧只留「抽题历史」文字。
- 已构建到 dist，版本号 20260823.3，明文密钥零出现。

## 大转盘微调 v20260823.4
- 用户截图要求「更多」面板里「不重复抽题」行文案与「抽题历史」标题左对齐，开关放到右侧挨着「重置本题库」。
- 调整 no-repeat-bar 结构：左侧为「抽过的题不再重复（已抽 x/y）」标题，右侧 .no-repeat-right 包含开关 + 重置按钮；去掉最大宽度、统一字号颜色，与下方历史行对齐。
- 已构建到 dist，版本号 20260823.4，明文密钥零出现。待部署强刷。
