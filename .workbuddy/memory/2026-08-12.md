# 2026-08-12

## 首页紧凑化调整（src/style.css）

用户反馈首页字体偏大、顶部 hero 区占空间过多，首屏只能看到约 3 个条目。已整体缩小字号与间距：

- hero 区：h1 30→22px，描述段 14→13px，NOTE 提示条 14→13px，各行 margin/padding 大幅压缩
- 模块切换按钮：高度 44→36px，字号 14→13px
- 搜索框：高度 46→38px，字号 15→14px
- 筛选按钮：科目 14→13px，分类 13→12px
- 内容卡片：标题 17→15px，padding 15→12px，margin 12→8px，封面 88→76px，链接摘要 13→12px
- 笔记卡片：摘要 14→13px，头部 12→11px

已 `npm run build` 更新 dist，部署后即生效。

## 个人中心 + 意见反馈（新增）

- 新增 `src/components/PersonalCenter.vue`：列表页右下角悬浮按钮（.pc-fab，仅非详情页显示），点开底部弹出面板。
  - 意见反馈：类型 chips（链接失效/内容错误/功能建议/其他）+ 内容 + 选填联系方式，提交到 `POST /h5/feedback/submit`（新文件 src/api/feedback.js）；接口不可用时降级为复制内容引导发邮箱（VITE_CONTACT_EMAIL，默认 monkeys.xu@qq.com）。
  - 我的收藏 / 浏览历史：localStorage 本地存储（src/utils/localStore.js，key: mp_favorites / mp_history，历史上限 50 条），点击条目 emit open 回 App.vue 调 openDetail。
  - 分享本站（复制链接）、联系与关于（公众号 VITE_OFFICIAL_ACCOUNT_NAME + 邮箱）、清除本地数据。
- App.vue：详情页加「☆ 收藏」按钮（fav-toggle），openDetail 成功后 addHistory + syncFavState；新增 openFromCenter。
- 后端需补 `/h5/feedback/submit` 接口（约定 res.code === 0 为成功）。

## H5 反馈接入后台（XukeProject，RuoYi 架构）

- 后台已有 sys_feedback 表 + 小程序接口 MpFeedbackController（/mp/feedback/submit，需登录）；feedbackType 原枚举 1功能建议 2BUG反馈 3账号问题 4其他。
- 新增 `ruoyi-admin/.../home/H5FeedbackController.java`：@Anonymous 匿名接口 `POST /h5/feedback/submit`，入参直接用 SysFeedback，强制 source=h5、nickname 默认 H5访客、status=0，标题为空按类型兜底；R.ok() code=0 与前端一致。
- SysFeedback 增加 `source` 字段（mp/h5/android/ios），feedbackType 扩展 5求资料 6链接失效；Mapper XML 的 resultMap/selectVo/list过滤/insert/update 均已同步。
- 迁移 SQL：`XukeProject/sql/H5反馈-source字段.sql`（ALTER 加 source 列，默认 mp），需在数据库手动执行。
- 前端 PersonalCenter.vue 反馈类型 chips 对齐后端枚举（链接失效6/求资料5/内容错误2/功能建议1/其他4），提交字段 feedbackType+title+content+contact。
- 本机无 mvn，后端未编译验证，需在 IDEA 里 rebuild。前端已 build 通过。

## 个人中心「支持本站」赞赏入口

- 菜单新增「支持本站」（heart 图标），页内文案：资料笔记免费分享 → 自愿赞赏 → 所得用于服务器/域名/存储运营。
- 收款码：public/assets/wepay.jpg，以 `${baseUrl}assets/wepay.jpg` 引用。
- 合规处理：全程使用「赞赏/支持」表述而非「捐赠/募捐」（个人无公开募捐资质），页脚声明自愿、非交易、不承诺回报。

- 18:22 赞赏区添加引导语：PersonalCenter.vue donate 视图中二维码上方新增 slogan「一杯奶茶的心意，是本站持续更新的最大动力」，style.css 新增 .pc-donate-slogan 样式（brand-dark 色、加粗），二维码上间距由 14px 调为 10px。
- 18:31 收款码已替换为微信官方赞赏码海报（wepay.jpg），赞赏区 UI 优化：去掉裸码时代的灰边框、海报宽度 200→232px、圆角 16px、投影改为品牌绿调；slogan 加两侧装饰线（flex+伪元素）；说明卡片改为白到 brand-soft 渐变+品牌色边框；提示文案改为「扫一扫/长按识别二维码即可赞赏」。vite build 通过。
- 18:34 用户将赞赏码换为 zanshang.jpg（微信官方赞赏码，方形白底+金色底部，图内自带 slogan），已拷入 public/assets 并更新引用；移除页面上重复的 slogan 及其样式；海报样式改为金色系（边框 #f0e2c0、投影 rgba(217,164,65,.18)），宽度 240px。
